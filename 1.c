#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(void)
{
	int flag=1;
	int y;
	while(flag!=0)
	{
		int y;
		y=3;
		flag=0;
		
	} 
 	y=4;
 	
	//y는 지역변수 내에서 정의된 변수이기 때문에 while문이 종료된 이후에는
	//y는 정의될 수 없다. 
	
	system("PAUSE");
	return 0;
	 
}

	 
	

